# LLM 路由

本上下文描述本地 LLM 请求路由产品中的核心业务概念，以及各概念之间的明确边界。

## Language

**本地路由器**：
位于请求方与上游 LLM 服务之间的本地中介，负责为请求选择可用凭证，并对可恢复的请求故障执行透明切换。
_Avoid_：客户端配置切换器、SDK 包装器

**路由会话**：
从 TUI 启动到退出之间，本地路由器能够接收和转发请求的一段连续运行期；TUI 退出即结束该会话。
_Avoid_：后台服务、常驻守护进程

**上游凭证**：
一个可独立参与路由并拥有自身可用状态的上游访问身份，由服务配置、API Key 和可选的地址覆盖共同确定；即使多个 Key 属于同一账号，它们仍是不同的上游凭证。
_Avoid_：账号、额度组、Key

**服务配置**：
可被多个上游凭证引用的上游服务定义，包含请求表面、默认地址、认证方式、额度验证和响应判定规则，但不包含具体 API Key。
_Avoid_：上游凭证、账号配置

**路由池**：
一组拥有相同请求表面、因而可以互相故障转移的上游凭证；不同路由池之间不互相切换。
_Avoid_：账号池、全局 Key 池

**请求兼容**：
同一个原始请求能够在不转换路径、协议、模型名或语义的情况下直接发送给多个上游凭证；兼容的请求表面可以包含多种协议。
_Avoid_：协议转换、模型映射

**额度不足**：
上游凭证的额度规则被响应明确命中后所处的不可选状态；瞬时限速、认证失败、超时和服务故障不属于额度不足。
_Avoid_：限速、不可用、所有 429 响应

**额度规则**：
针对一种上游服务配置的响应判定条件，用于识别额度不足；条件可以基于状态码、响应头、结构化错误码或响应体。
_Avoid_：通用 429 判断

**凭证状态**：
路由器对上游凭证当前可选择性的持久判断，取值为未知、可用、额度不足、凭证无效或已禁用；瞬时请求故障不属于凭证状态。
_Avoid_：健康状态、瞬时故障

**透明重试**：
在响应对请求方可见之前，因上游凭证额度不足而改用同一路由池中另一凭证重放原请求；响应一旦可见就不再重试。
_Avoid_：流式续传、瞬时故障重试

**当前凭证**：
路由池中承接正常请求的可用上游凭证；只有自动故障转移或用户手动选择才会改变当前凭证。
_Avoid_：轮询节点、负载均衡节点

**验证间隔**：
路由池为额度不足凭证设定的固定重复验证周期，默认五分钟且可由用户配置。
_Avoid_：恢复时间、退避周期

**额度验证**：
使用路由池指定的模型请求判断上游凭证是否恢复额度的探测；它只产生可用、额度不足、凭证无效或无结论的结果，不代表一般服务健康检查。
_Avoid_：健康检查、连接检查

**本地端点**：
请求方访问某一路由池的稳定本地 Base URL，由共享监听地址和该池的唯一名称组成。
_Avoid_：上游地址、每池端口
